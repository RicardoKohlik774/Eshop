You can interact with this program by typing the available command in the console. Such as:
Login ---> To get access either user or admin interface, you need to log in first by typing "login" into the console and then
           inputting an 8-digit code, which serves as your login id. To get access to the admin interface, log in with the id = "00000000".